# VoiceSpreader Android v1.2.4.0

## 手机增强控制

- 新增 protocol 3 控制通道：全屏触摸板、双指滚动、鼠标点击和常用快捷键。
- 新增“编辑快捷键”入口，可在手机端保存自定义名称、主键和修饰键组合。
- 支持接收 Windows 应用白名单并一键启动电脑端应用。
- 新增应用内文件共享面板，可从相册/已授权目录选择文件并拖入触摸板共享区。
- 文件通过独立 TCP file channel 流式传输，不经过 Base64，不阻塞音频连接，并支持 SHA-256 校验与断点续传。
- 新增相机扫码、相册扫码和手机拍照传电脑；照片只在内存中处理，不写入手机相册。
- 电脑发起的扫码/拍照请求通过前台服务通知进入确认页面，符合 Android 后台启动限制。
- 版本号统一为 `1.2.4.0`。

## 兼容性

- 保留旧版 protocol 2 音频连接；protocol 3 功能连接与音频连接并行工作。
- 使用 Photo Picker、Storage Access Framework 和 CameraX，按用户操作申请权限。
