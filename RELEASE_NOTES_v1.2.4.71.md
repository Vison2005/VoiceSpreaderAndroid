# VoiceSpreader Android v1.2.4.71

- 版本号更新为 `1.2.4.71`（versionCode `102471`）。
- 单指移动补发 MotionEvent 中最近的历史采样点，减少高刷新率屏幕上的光标跳动和卡顿。
- 连接正常时移动帧直接进入独立写入队列，减少高频事件的主线程分配。
- 竖屏设置图标收紧顶部安全间距；横屏保持原有布局，并继续避开显示切口。
