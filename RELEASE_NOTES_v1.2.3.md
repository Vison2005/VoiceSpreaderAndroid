# VoiceSpreader Android v1.2.3

## 设备互联管理

- 配对成功后自动保存电脑信息，下次可从“已保存的电脑”直接连接。
- 电脑地址变化时，直连失败会自动在局域网中按稳定会话身份重新定位。
- Windows 断线或重启后，前台服务会接收稳定会话广播并静默恢复连接，无需再次确认。
- 同一 Android 服务只保留一个活动电脑连接，切换设备会先结束旧连接。
- 支持移除不再使用的已保存设备；二维码和配对码仍可用于添加新电脑。
