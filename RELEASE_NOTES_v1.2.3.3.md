# VoiceSpreader Android v1.2.3.3

## 后台连接恢复
- 保持连接由带通知的前台服务托管，返回桌面、锁屏或移除任务不会主动结束音频链路。
- 看门狗闹钟在服务被系统回收后重新拉起连接服务，并恢复上一次配对信息。
- 看门狗触发时会检查并恢复 UDP 主动唤醒监听线程，避免监听线程异常退出后无法被 Windows 唤醒。
- 用户点“断开”仍会清除恢复标记，不会在主动断开后自动重连。

## 版本
- 应用内版本号统一为 `1.2.3.3`。
