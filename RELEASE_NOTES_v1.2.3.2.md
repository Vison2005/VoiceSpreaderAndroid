# VoiceSpreader Android v1.2.3.2

## 后台连接与主动重连

- 连接服务继续由前台服务托管，退到桌面、锁屏或移除任务后不主动断开。
- 增加系统闹钟看门狗；部分 ROM 回收连接进程后，会自动重新拉起服务并恢复上次配对。
- 接收 Windows 主动重连唤醒包，使用已保存配对信息自动连接，不需要再次验证。
- 用户主动断开时清除恢复标记，避免断开后被看门狗重新连接。
- 应用内版本号统一显示为 `1.2.3.2`。
