# VoiceSpreader Android v1.2.1

## 主要变化

- 握手携带稳定设备 ID，电脑端可以在手机重连后恢复独立路由设置。
- Windows 声音播放增加 80 ms 统一预缓冲，降低多台设备因网络到达时间不同产生的起播偏差。
- 使用共享音频帧序号检测丢包和重叠包；短缺口自动补静音，重复数据自动裁剪。
- 扩大应用内播放队列，并保持有界丢旧策略，避免网络拥塞导致延迟持续累积。
- 保留手机作为电脑麦克风、手机作为电脑扬声器及双向同时传输能力。
- 麦克风采集线程改为按连接代次隔离，快速停止、重连时旧线程不会干扰新会话。
- 修复重连期间网络锁未释放的问题，并增强播放停止时对阻塞写入的解除能力。
- 使用应用本地随机设备 ID 代替系统硬件标识，保持重连识别的同时减少不必要的设备信息访问。

## 说明

- 多设备协调由 Windows v1.2.1 主机完成；每台 Android 设备仍只连接一台电脑。
- Windows 声音在 VoiceSpreader 应用内播放，不会注入 Android 的系统全局音频设备。
